<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.desarrollouf2_enrique_ruiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Consultar"></activity>
        <activity android:name=".Insertar" />
        <activity android:name=".Activity2" />

        <service
            android:name=".servicio1"
            android:enabled="true"
            android:exported="true" />
        <service android:name=".servicio" />

        <activity android:name=".Activity3" />
        <activity android:name=".TablaUsuarios" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".servicio" />
    </application>

</manifest>